{
  "name": "indra-net",
  "version": "1.0.0",
  "private": true,
  "description": "Indra Net — Premium e-commerce on Cloudflare Pages + D1",
  "scripts": {
    "dev": "wrangler pages dev public --d1=DB",
    "deploy": "wrangler pages deploy public",
    "db:create": "wrangler d1 create indra-net-db",
    "db:schema": "wrangler d1 execute indra-net-db --file=schema.sql --remote",
    "db:schema:local": "wrangler d1 execute indra-net-db --file=schema.sql --local",
    "db:seed": "wrangler d1 execute indra-net-db --file=seed.sql --remote",
    "db:seed:local": "wrangler d1 execute indra-net-db --file=seed.sql --local",
    "db:reset": "npm run db:schema && npm run db:seed",
    "db:reset:local": "npm run db:schema:local && npm run db:seed:local",
    "setup": "npm run db:create && npm run db:reset"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}