{
  "name": "indra-net",
  "version": "1.0.0",
  "private": true,
  "description": "Indra Net — Premium e-commerce on Cloudflare Pages + D1",
  "scripts": {
  "dev": "wrangler pages dev public --d1=DB",
  "build": "echo 'No build step required'", 
  "deploy": "wrangler pages deploy public",
},
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}
